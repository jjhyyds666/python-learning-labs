[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "greet-cli-lab"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
greet-lab = "greet_cli.cli:main"