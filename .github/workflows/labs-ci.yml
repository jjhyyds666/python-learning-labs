name: Labs CI

on:
  push:

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - name: Download repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install development tools
        run: python -m pip install -r ruff-lab/requirements-dev.txt

      - name: Run tests
        run: python -m pytest -q ruff-lab

      - name: Run Ruff lint
        run: ruff check --config ruff-lab/pyproject.toml ruff-lab

      - name: Check Ruff formatting
        run: ruff format --check --config ruff-lab/pyproject.toml ruff-lab
